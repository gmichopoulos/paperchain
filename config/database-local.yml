development:
  adapter: postgresql
  encoding: utf8
  host: localhost
  database: paperchain_development
  pool: 5
  username: paperchain
  password:

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  host: localhost
  database: paperchain_test
  pool: 5
  username: paperchain
  password:

production:
  adapter: postgresql
  encoding: utf8
  host: localhost
  database: paperchain_production
  pool: 5
  username: paperchain
  password:
